default:
    description: 'Development build'
    tasks:
        - concurrent:process_clean_dev
        - concurrent:process_sass_dev
        - concurrent:process_jshint
        - concurrent:process_scsslint
        - concurrent:process_uglify
        - concurrent:process_codekit
        - concurrent:process_copy_dev

prod:
    description: 'Production build'
    tasks:
        - concurrent:process_clean_prod
        - concurrent:process_sass_prod
        - concurrent:process_jshint
        - concurrent:process_scsslint
        - concurrent:process_uglify
        - concurrent:process_codekit
        - concurrent:process_copy_prod